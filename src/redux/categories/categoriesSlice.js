import { createSlice } from '@reduxjs/toolkit';

const categoriesSlice = createSlice({
  name: 'categories',
  initialState: 'Under construction',
  reducers: {
    // No additional reducers needed
  },
});

export default categoriesSlice.reducer;
